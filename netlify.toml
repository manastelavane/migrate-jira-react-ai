[build]
  command = "npm install && cd apps/next && npm install && npm run build"
  publish = "apps/next/.next"

[build.environment]
  NODE_VERSION = "20"
  NETLIFY_NEXT_PLUGIN_SKIP = "false"

[[plugins]]
  package = "@netlify/plugin-nextjs"
